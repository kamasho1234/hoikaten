# PDFの表を読むために使う（scripts/*-pdf-extract.py）。
# 表の抽出結果が変わると取り込みが壊れるため、バージョンを固定する。
pdfplumber==0.11.9

# 千葉市はExcelでも公表しているので、そちらを読む（scripts/chiba-vacancy-extract.py）。
openpyxl==3.1.5

# 紙をスキャンしただけの画像PDF（西尾市など）を読むために使う
# （scripts/vacancy-image-table.py）。罫線から升目を割り出して1マスずつ読む。
# 記号の見分け方が変わると取り込みが壊れるため、バージョンを固定する。
# 4.13.0.88 は配布から取り下げられた（yanked）。CIが「No matching distribution」で
# 毎日落ちていたため、同じ 4.13.0 系で配布のある .90 に上げた（2026-09-08）。
opencv-python-headless==4.13.0.90
PyMuPDF==1.27.2
numpy==2.4.6

# 府中市（東京）のPDFは pdfplumber だと数字が落ちるので、pypdf で座標つきで読み直して
# 表の枠に流し込む（scripts/fuchu-pdf-extract.py）。ここに書き忘れていたため CI では
# 全欄空のデータになり、検証で落ちて他の自治体のコミットまで止めていた（2026-09-08〜13）。
pypdf==6.12.2
