## 2026-09-18 エージェントの「○」報告は、ファイルの中身と git の状態で確かめる
一時保育43件の収集で、1体が荒川・文京・千代田を「3件とも照合○、evidence は全部公式の原文」と詳しい数字つきで
報告したが、実物は **evidence が空のスタブ3件で URL も作り物**（3区とも `azukariseikatsu.html`）。しかも
先に完成していた荒川・文京を上書きして消していた。復元は `git fsck --unreachable --no-reflogs` の blob から
できた（直前に `git add` していたため）。
**受け取り方**: 報告を読まずに、まず `verify-fact-records.py` を自分で回し、`wc -c` でサイズを見る。
評価が済んだレコードはその場で `git add` する（上書きされても index の blob から戻せる）。
**出し方**: 1自治体1体。3〜4件持たせた14体のうち10体が「トークン制約」で途中停止し、完走しても
他のエージェントのファイルを「掃除」で消した。

## 2026-09-18 quote が本文にあっても、値は quote を超える
機械照合を通った42件を「値 ← quote」で通読したら、38件に quote に無い数字や条件が混じっていた
（施設数、減免の金額、申込期日、「施設と保護者の契約で決まる」のような手本の言い回し）。
古いページ（令和6年度の試行事業、記者発表、予算資料、政策会議資料）を令和8年度の案内として引いた自治体も5つ。
**照合は「quote が存在する」ことしか保証しない。値が quote から読めるかは人が読むしかない。**
通読は `pages.py` で参照ページ全部をテキストに落とし、疑った値ごとに grep して確かめると1自治体10分で済む。

## 2026-09-14 事実シート方式でも「数字の使いどころ」は壊れる
奈良市のコラム41本を、使ってよい数字だけを書いた facts.md を渡して4体に書かせた。数字は1つも facts の外から
出なかったが、通読すると **事実の取り違えが20か所以上**（4月入所の申込月と通知方法、育休の期限の主語、
副食費で数える子の範囲、減点の条件の裏返し、別の制度の金額の流用）。機械チェックで拾えたのは構成の不足だけ。
**数字の許可リストは必要条件であって十分条件ではない。書かせたものは全文を読む。**
読むときは「この数字はどの制度の、誰についての数字か」を1文ずつ facts と照らす。

## 2026-09-14 検証は自治体ごとに戻す。1件で全体を止めない
空き状況の日次バッチは「取り込みは自治体ごとに continue-on-error」なのに、最後の `vacancy:verify` だけが
全体を止める作りだった。府中市1件（CI に pypdf が無い）で **9/8〜9/13 の6日間、50以上の自治体の新データが
1件も公開されなかった**。ワークフローは verify が出す `VERIFY_FAILED_SLUGS` を読んで、その JSON だけ
`git checkout` で戻し、残りを再検証してコミットする形にした。
**「壊れたものを出さない」ゲートは、壊れたものだけを止める粒度で作る。**

## 2026-09-14 CI の Python 依存はローカルの pip freeze と突き合わせる
府中市の抽出スクリプトは pypdf を「無ければ None」で黙って飛ばしていた。ローカルには入っていたので
気付かず、CI では全欄空のデータを出して検証で落ちた。**新しい import を足したら requirements.txt にも
足す。無いと成り立たない依存は、無いときに fail させる（黙って空を返さない）。**

## 2026-09-11 サブエージェントに「委譲」させると仕事が消える
14体に自治体を割り当てたら、数体が担当を配下に「委譲」して報告待ちになり、その分は1件も直っていなかった。
さらに他グループの担当に手を出して衝突しかけた。**「サブエージェントを使わず自分で Read/Edit」「1件ずつ最後まで」
「終わったら tsc」を最初から書く。** 結果は必ず機械照合で確かめ、報告は信用しない（[[feedback_agent_factcheck]] と同じ）。
記事の本数が減っていないか（articles:verify の数）も見る。

## 2026-09-11 push 先のブランチを間違えた
`git push origin master` したが、このリポジトリの本番ブランチは **main**。
`master` は初回コミットだけの古い stub で、Vercel は見ていない。
push のあと本番に反映されず、`git branch -vv` で気付いた。
**push の前に `git branch --show-current` を見る。本番反映の確認は必ず正しいURLで行う**
（このときは `/articles/uto-guide` という存在しないURLで待っていて、404を「未反映」と読んでいた。
自治体記事は `/<slug>/articles/<記事slug>`）。

# 教訓

## push 先のブランチを毎回確認する（2026-08-21）

**何があったか**: `git push origin master` を実行して成功していたが、
このリポジトリの本番ブランチは `main` だった。ローカルに残っていた古い
`master`（initial commit のまま）へ push していたので、
「push 成功」なのに Vercel は何も拾わず、本番に20分以上反映されなかった。

**なぜ間違えたか**: セッション開始時のコンテキストに出る
「Current branch: master」は、ホームディレクトリ（`C:\Users\kamas`）の
別のリポジトリの状態だった。プロジェクト側のブランチとは関係がない。

**どうするか**: push の前に `git branch --show-current` で今いるブランチを
確認し、そのブランチ名で push する。push のあとは
`git log --oneline -1 origin/<branch>` で本当に届いたかを見る。
本番URLへの curl だけでは「デプロイが遅い」のか「そもそも届いていない」のかが
分からないので、まずリモートの ref を見ること。

## 記事ファイルは register-all.ts に import しないと出ない（2026-09-07）

`src/lib/articles/<slug>.ts` を作っただけでは記事は公開されない。
`register-all.ts` に `import "./<slug>";` を足して初めて `registerArticles()` が走る。

**足し忘れても型チェックもビルドも通る。** そのページだけが静かに404になる。
実際に7自治体・72本の記事が、この抜けで長い間公開されていなかった。

新しく記事ファイルを作ったら、必ず `npm run articles:verify` を通すこと
（`npm run build` の前に自動で走るようにしてある）。
記事を足したら、本番URLにcurlして200を確かめるところまでやる。

## 記事slugは半角英数とハイフンだけにする（2026-09-07）

見た目が同じでも別の文字が混ざるとページが出ない。
実際に `lotterу` の у がキリル文字（U+0443）になっていた記事があった。
日本語のslug（`途中入所-guide`）も同じく出ない。

`npm run articles:verify` で確認するようにしたが、
**記事を足したら本番URLにcurlして200を確かめる**のを省かないこと。
今回もそれで見つけた。

## 公開したあとにも、出典URLから照合し直す（2026-09-07）

記事を書くときの照合は「その場の資料」に対するもので、
**公開中の記事が今も正しいか**は別の話。

実際、以前に作られた保育料の記事47件が、自治体の公表していない
「世帯年収別の目安表」を載せていた。11自治体がまったく同じ表を共有し、
出典URLは1件も無かった。書いた時点では誰も気付かない形で残っていた。

`python scripts/verify-fee-articles.py` を定期的に走らせる。
記事に載せたURLから資料を取り直し、金額がその資料に出てくるかを見る。
「合わない」が1件でも出たら、その自治体の資料を目で確かめる。

## 原典が計算例を載せているときは、それを検証ケースにする（2026-09-08）

笛吹市の利用調整基準には計算例が載っていた。
「（父）家庭外労働100点 ＋（母）自営業（従業員）75点 ＝ 合計175点」。
これをそのまま `scripts/smoke-scoring.ts` のケースにすれば、
採点方式（sum/min）も選択肢の点数も、原典の答えと突き合わせて確かめられる。

自分で組み立てたケースは「自分の読み取りが正しい」という前提の上に乗っているが、
原典の計算例は自治体が出した答えなので、その前提ごと確かめられる。
新しい自治体を足すときは、まず原典に計算例が無いか探す。

## 固定した依存が消えることがある。CIの失敗は放置しない（2026-09-08）

`opencv-python-headless==4.13.0.88` が配布から取り下げられ（yanked）、
毎朝の空き状況更新が5日以上失敗し続けていた。
バージョン固定は再現性のために要るが、**消えたときに気付く仕組みが無かった**。

作業の切りのいいところで `gh run list` を見る。
失敗が続いていたら、その場で原因まで見る。

## 直し方が他を壊すなら、直さずに記録して戻す（2026-09-08）

西尾市の読み取りの誤り（空欄6マスが「クラスなし」）の原因は測れたが、
その直し方は荒川区と吹田市を壊した。
`npm run vacancy:verify` が検出したので変更を戻した。

**部分的に良くなるからといって、他を壊す変更を入れない。**
測った内容（どこにどれだけインクが出ていたか）を残しておけば、
次に直すときの出発点になる。

## 同じ名前の自治体がいる。登録前に必ず確かめる（2026-09-09）

日本には同じ名前の市町村がある。slug をそのまま付けると別の自治体を上書きする。

これまでに当たったもの:
- 中野市（長野）と中野区（東京）… `nakano` は中野区。市は `nakano-nagano`
- 伊達市（北海道）と伊達市（福島）… 福島が `date-fukushima`。北海道は `date-hokkaido`
- 大仙市（秋田）… `daisen` は空いていた。「鳥取に大山町がある」は私の思い込みで実在しなかった

**手順**: 新しい自治体を足す前に
`grep -rl "<自治体名>" src/lib/data/*.ts` と
`ls src/lib/data/<slug>.ts src/lib/vacancy/<slug>.json` を見る。
空き状況データが先に slug を使っていることもあるので、そちらも確認する。
思い込みで「同名がある／ない」と決めない。

## CI と手元で OCR の学習データが違うと、手元で通る取り込みが CI で落ちる（2026-09-16）

姶良市・松戸市の画像 PDF は手元では読めるのに CI では施設名の照合に失敗し続けていた。
手元の tesseract は tessdata_best の jpn（14MB）、CI は apt の tesseract-ocr-jpn（tessdata_fast 2.4MB）だった。
検証（`vacancy:verify`）は手元の結果を基準にしているので、**読み取りに使う道具は CI でも手元と同じ版をコミット固定で入れる**。
「手元では通る」は CI が通る根拠にならない。落ちたら環境の差を先に疑う。

## マスの大きさに比例するしきい値は、行の高さが違う行で静かに壊れる（2026-09-16）

姶良市の画像表で、施設名が2行になる高い行（2施設）だけ ×が「空欄」と判定され、8月分から null のまま公開されていた。
`inkRatio` は「マスの面積 × 割合」なので、高い行ほど多くのインクを要求する。
通常の行は 133画素 vs 必要121画素と**ぎりぎり通っていた**だけだった。
画像表の自治体を足すときは、取り込んだ記号の数を PDF と目視で突き合わせる（null が記号の位置に出ていないか）。

## 自治体は「公開期間」を決めて表を下げる。404 やリンク不在は移転とは限らない（2026-09-16）

荒川区（毎月1〜10日）・伊達市（毎月1〜5日）・盛岡市（選考中は下げる）・東大和市（申請期間の合間は「更新準備中」）・
桑名市（前々月末日にお知らせに載せ、締切後に消す）・紫波町（月ごとに記事を作り直す）。
移転先を探す前に、ページ本文に「公開を停止」「公開は終了」「更新準備中」「公開期間」の文言が無いか読む。
あれば失敗ではなく見送り（`pausedPattern`／`pdf.optional`）にして、前回のデータを残す。

## 2026-09-18 事実レコードを集めさせるときは「引用の照合」だけでは足りない

就労証明書の自治体別レコード87件を31体のエージェントに集めさせた。機械照合（URL 200／quote が本文にある）を
通ったレコードの中に、次の4種類の壊れ方が混ざっていた。どれも quote は本物で、値が間違っている。

1. **quote と値が対応していない**: ページ名や見出し・ファイル名を quote にして、値は別のところから書く（柏崎・川崎・新潟・羽村・西東京…）
2. **手本の値を流用して quote だけ自分のページから取る**: 大阪市の注意書き7本が静岡市・金沢市・神戸市の notes にそのまま入っていた
3. **フィールドの意味を取り違える**: 有効期限に「直近3か月の実績」「父母それぞれ必要」「年1回の現況届」を入れる（浜松・神戸・北九州）
4. **様式を開かずに判定する**: 国の記載要領の表題「【就労証明書（簡易版）】」を見て独自様式と判定（静岡・富士宮）、ページの文言だけで standard（南国・柏崎）

直し方（`scripts/verify-shurou-records.py` に入れた）:
- quote がページの `<title>` に含まれる／ファイル名（PDF・KB…）なら弾く
- 同じ文が2つ以上の自治体に出ていたら弾く（流用の検出）
- 配列フィールドに文字列を入れると1文字ずつ分割される事故 → 要素が4文字未満なら弾く
- standard 以外は differences 必須
- それでも 3 と 4 は機械では見えない。**全件を「値 ← quote」の形で出して人が読む**（`scripts/dump-shurou-records.py`）。87件で20件を書き直した

運用の教訓:
- general-purpose のエージェントは「トークン制約」「時間制約」を理由に途中で止まる。残量が1,000万あっても止まる。
  依頼文に「制約は無い・途中報告で止まらない」と書いても、3〜4件で報告して idle になる。**1体あたり3〜4件**にして数を増やすほうが早い
- 途中報告を受けて返事をすると、そのたびに再開はするが、同じ理由でまた止まる。返事より新しい体を出すほうが安い
- 停止を頼んだ体が、返事より前に走っていた処理でファイルを消すことがある（姫路・西宮）。停止後に `ls records/` を必ず数える
- 自治体サイトの落とし穴: kodomo.city.fukuoka.lg.jp は短い UA と urllib を 403 にする（curl＋Chrome UA なら通る）、
  川崎市は令和9年度様式を別ページ（0000154328）に置く、福岡市の旧ページは href を抜いた「（使用不可）」ページ
